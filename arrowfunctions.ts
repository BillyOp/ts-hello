// Defining functions the JS way
let logo = function(message){
    console.log(message)
}


// Defining functions using ARROW FUNCTIONS
let doLog = (message) => console.log(message);
let doLog2 = () => console.log()